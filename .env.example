# Beo-agents — root env is optional.
# Secrets live in EACH agent folder. Never share a bot token or key across agents.
#
#   copy agents/social-beo/.env.example agents/social-beo/.env
#
# On Linux servers, set these if the volume files should match a host user:
# HERMES_UID=1000
# HERMES_GID=1000
